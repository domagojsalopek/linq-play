using linq_play.model;
using linq_play.posts;

internal class Program
{
    private static void Main(string[] args)
    {
        //new LinqFromMethods().Run();
        new Projections().Run();
    }
}